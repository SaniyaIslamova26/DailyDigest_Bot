Тема проекта:
Среда выполнения: VS Code
Язык программирования: Python
Инструменты: Telegram, BotFather
Структура проекта:
```
DAILY_DIGEST_BOT/
│
├── 📁 __pycache__/               # КЭШ Python (автоматически генерируется)
│   ├── config.cpython-311.pyc    # Кэшированные байт-код файлы
│   ├── db.cpython-311.pyc
│   ├── digest.cpython-311.pyc
│   ├── scheduler.cpython-311.pyc
│   └── sources.cpython-311.pyc
│
│
├── 📁 venv/                      # ВИРТУАЛЬНОЕ ОКРУЖЕНИЕ Python
│   ├── Include/
│   ├── Lib/                      # Установленные библиотеки
│   ├── Scripts/                  # Исполняемые файлы
│   └── pyvenv.cfg               # Конфигурация окружения
│
├── 📄 config.py                  # КОНФИГУРАЦИЯ бота (токены)
├── 📄 daily_digest.db           # БАЗА ДАННЫХ SQLite
├── 📄 db.py                      # МОДУЛЬ РАБОТЫ с БД
├── 📄 digest.py                  # МОДУЛЬ ФОРМИРОВАНИЯ дайджеста
├── 📄 main.py                    # ГЛАВНЫЙ ФАЙЛ бота
├── 📄 requirements.txt           # ЗАВИСИМОСТИ проекта
├── 📄 scheduler.py               # МОДУЛЬ ПЛАНИРОВЩИКА
└── 📄 sources.py                 # МОДУЛЬ ПАРСИНГА новостей
```


## Общее описание
Telegram-бот для персонализированного политического дайджеста из 85+ российских СМИ. Бот предоставляет пользователям возможность выбрать тематические категории и получать ежедневную подборку из 12 главных новостей, а также имеет реферальную систему и админ-панель.

## Архитектура проекта

### 1. **main.py** - Основной модуль бота
**Назначение**: Главный файл приложения, содержащий логику обработки команд и состояний бота.

#### Ключевые компоненты:
- **Импорты и конфигурация**:
  ```python
  # Импорт библиотек aiogram для работы с Telegram API
  # Инициализация бота и диспетчера
  # Настройка сессии с увеличенным таймаутом для стабильной работы
  ```

- **Класс States (StatesGroup)**:
  ```python
  # Управление состояниями FSM (Finite State Machine)
  # Используется для отслеживания процесса выбора категорий
  ```

- **Функции клавиатур**:
  ```python
  def main_keyboard()    # Главное меню с кнопками
  def categories_keyboard() # Инлайн-клавиатура для выбора категорий
  ```

- **Обработчики команд**:
  ```python
  @dp.message(Command("start"))      # Инициализация пользователя
  @dp.callback_query()              # Обработка нажатий инлайн-кнопок
  @dp.message()                     # Обработка текстовых команд из меню
  ```

- **Админ-функции**:
  ```python
  async def stats()                 # Статистика для администратора
  ```

### 2. **config.py** - Конфигурационный файл
**Назначение**: Хранение чувствительных данных и конфигурационных параметров.

```python
# Токен бота и ID администратора
# Всегда должен быть в .gitignore для безопасности
```

### 3. **db.py** - Работа с базой данных
**Назначение**: Управление SQLite базой данных пользователей.

#### Ключевые функции:
```python
def init_db()           # Создание таблицы пользователей
def add_user()          # Добавление нового пользователя
def update_categories() # Обновление выбранных категорий
def get_user_categories() # Получение категорий пользователя
def get_all_subscribers() # Получение всех активных подписчиков
def unsubscribe_user()  # Отписка пользователя
```

#### Структура таблицы:
- `user_id` - уникальный ID пользователя Telegram
- `categories` - JSON-список выбранных категорий
- `subscribed` - статус подписки (1/0)
- `referrer_id` - ID реферера (для реферальной системы)
- `unlimited_until` - дата до которой безлимитный доступ

### 4. **sources.py** - Парсинг новостных источников
**Назначение**: Загрузка и обработка RSS-лент новостных СМИ.

#### Ключевые компоненты:
- **CATEGORIES_DISPLAY** - словарь отображаемых названий категорий
- **RSS_FEEDS** - словарь с RSS-ссылками для каждой категории (85+ источников)
- **KEYWORDS** - ключевые слова для фильтрации новостей по категориям

#### Основные функции:
```python
def parse_rss()         # Парсинг RSS-ленты
def clean_text()        # Очистка HTML-тегов из текста
def get_news_for_category() # Получение новостей для конкретной категории
def get_news_stats()    # Статистика по количеству новостей
```

### 5. **digest.py** - Формирование дайджеста
**Назначение**: Создание персонализированного дайджеста новостей.

```python
def get_daily_digest()  # Основная функция формирования дайджеста
# Алгоритм:
# 1. Сбор всех новостей по выбранным категориям
# 2. Дедупликация по ссылкам
# 3. Сортировка по времени публикации
# 4. Отбор топ-12 новостей
# 5. Форматирование в HTML для отправки
```

### 6. **scheduler.py** - Планировщик рассылки
**Назначение**: Организация автоматической ежедневной рассылки.

```python
async def send_daily_digest() # Рассылка дайджеста всем подписчикам
def start_scheduler()         # Запуск планировщика на 9:00 МСК ежедневно
```

### 7. **requirements.txt** - Зависимости проекта
**Назначение**: Список необходимых Python-пакетов.

```
aiogram==3.13.1       # Основная библиотека для работы с Telegram API
feedparser==6.0.11    # Парсинг RSS-лент
apscheduler==3.10.4   # Планировщик задач
pytz==2024.1          # Работа с часовыми поясами
aiohttp==3.9.1        # Асинхронные HTTP-запросы
```

## Поток данных в приложении

1. **Инициализация**:
   ```
   main.py → init_db() → db.py (создание БД)
   ```

2. **Запуск бота**:
   ```
   asyncio.run(main()) → start_polling() + start_scheduler()
   ```

3. **Обработка команды /start**:
   ```
   Пользователь → /start → cmd_start() → 
   add_user() → выбор категорий → save()
   ```

4. **Ежедневная рассылка**:
   ```
   scheduler (9:00) → send_daily_digest() → 
   get_all_subscribers() → get_daily_digest() → 
   get_news_for_category() → parse_rss()
   ```

5. **Запрос дайджеста**:
   ```
   Пользователь → "Дайджест сейчас" → 
   get_user_categories() → get_daily_digest()
   ```


## Рекомендации по развертыванию

1. Установите зависимости: `pip install -r requirements.txt`
2. Создайте файл `config.py` с вашим токеном бота/Telegram ID (для статистики)
3. Запустите бота: `python main.py`


